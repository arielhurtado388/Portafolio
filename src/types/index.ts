export type Acerca = {
  id: number;
  titulo: string;
  subtitulo: string;
  descripcion: string;
};

export type Experiencia = {
  id: number;
  cargo: string;
  empresa: string;
  fecha: string;
  modalidad: string;
  listado: string[];
};
